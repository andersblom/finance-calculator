import React, { Component } from 'react';

export default class WorksheetPiechart extends Component {
  render() {
    const { items } = this.props;
    return (
      <svg>
        hi
      </svg>
    );
  }
}
